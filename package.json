{
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "packageManager": "npm@10.5.0"
}
